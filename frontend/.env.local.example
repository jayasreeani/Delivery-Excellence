NEXT_PUBLIC_API_URL=/api

# Dynamic API — connect directly to PostgreSQL (serverless on Vercel)
DATABASE_URL=postgresql://delivery:delivery123@localhost:5432/delivery_platform
DATABASE_SSL=false

# Auth
JWT_SECRET=change-me-in-production-use-long-random-string
JWT_EXPIRES_IN=24h

# Optional: proxy to separate Express backend instead of embedded API
# BACKEND_URL=https://your-backend.onrender.com
